plugins {
    id "org.jetbrains.kotlin.platform.native"
}

sourceSets.main.component {
    target 'host', 'wasm32'
    outputKinds = [ KLIBRARY ]
}

dependencies {
    expectedBy project(':commonLib')
}
